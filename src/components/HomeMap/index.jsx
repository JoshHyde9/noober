import React from "react";
import { View, Text } from "react-native";

export const HomeMap = (props) => {
  return (
    <View
      style={{
        height: 300,
        backgroundColor: "#ddd",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>TODO: Create a map</Text>
    </View>
  );
};
